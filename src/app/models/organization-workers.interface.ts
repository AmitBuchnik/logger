import { Organization } from "./organization.interface";
import { Worker } from "./worker.interface";

export interface OrganizationWorkers {

    organizations: Organization[];

    workers: Worker[];
}